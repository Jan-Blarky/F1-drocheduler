name: F1 schedule

on:
  schedule:
    # Израиль летом UTC+3, зимой UTC+2, поэтому ловим оба варианта:
    # 03-05 UTC — понедельничный анонс (06:00 IL), 16-18 UTC — напоминание (19:00 IL).
    # Лишние запуски безвредны: бот сам решает, наступил ли момент.
    - cron: '0 3,4,5,16,17,18 * * *'
  workflow_dispatch:
    inputs:
      mode:
        description: 'Что сделать'
        type: choice
        default: dry-run
        options:
          - dry-run   # показать в логе, что бот сделал бы сейчас
          - check     # проверить доступ к группе и право на закреп
          - send      # обычный боевой запуск
          - announce  # отправить и закрепить анонс этапа из поля round
          - test-pin  # прогнать цикл закрепа на одноразовом сообщении
      round:
        description: 'Номер этапа для режима announce'
        type: string
        default: ''

permissions:
  contents: write

concurrency:
  group: f1-schedule
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - run: pip install --quiet tzdata

      - name: Run bot
        env:
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          MODE: ${{ github.event.inputs.mode }}
          ROUND: ${{ github.event.inputs.round }}
        run: |
          case "$MODE" in
            check)    python bot.py --check ;;
            dry-run)  python bot.py --dry-run ;;
            announce) python bot.py --force-announce "$ROUND" ;;
            test-pin) python bot.py --test-pin ;;
            *)        python bot.py ;;
          esac

      - name: Save state
        run: |
          if ! git diff --quiet -- state.json; then
            git config user.name  'f1-drocheduler'
            git config user.email 'actions@github.com'
            git add state.json
            git commit -m 'state: отмечены отправленные сообщения'
            git push
          fi
